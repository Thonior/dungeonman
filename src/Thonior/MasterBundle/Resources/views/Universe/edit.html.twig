{% extends 'ThoniorMasterBundle::base.html.twig' %}

{% block body -%}
    <h1>Universe edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a class="manager" data-path="{{ path('universe') }}" href="#universe">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
