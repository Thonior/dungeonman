{% extends 'ThoniorMasterBundle::base.html.twig' %}

{% block body -%}
    <h1>Race creation</h1>

    {{ form_start(form, {'attr': {'id': 'form'}}) }}
    {{ form_errors(form) }}
    {{ form_end(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('race') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
