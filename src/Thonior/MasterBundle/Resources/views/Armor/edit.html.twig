{% extends 'ThoniorMasterBundle::base.html.twig' %}

{% block body -%}
    <h1>Armor edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('armor') }}">
            Back to the list of armors
        </a>
    </li>
    <li>
        <a href="{{ path('item') }}">
            Back to the list of items
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
